import MainLayout from '../layouts/main.layout';

const ProfilePage = () => {
  return (
    <MainLayout pageTitle='Profile' actions={[]}>
      .
    </MainLayout>
  );
};

export default ProfilePage;
