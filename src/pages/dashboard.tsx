import MainLayout from '../layouts/main.layout';

const DashboardPage = () => {
  return (
    <MainLayout pageTitle='Dashboard' actions={[]}>
      .
    </MainLayout>
  );
};

export default DashboardPage;
