import MainLayout from '../layouts/main.layout';

const QuotationsPage = () => {
  return (
    <MainLayout pageTitle='Quotations' actions={[]}>
      .
    </MainLayout>
  );
};

export default QuotationsPage;
